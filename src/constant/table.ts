import type { MRT_PaginationState } from "material-react-table";

export const INITIAL_PAGE_INFO: MRT_PaginationState = {
  pageIndex: 0,
  pageSize: 20,
};

export const INITIAL_DRAWER_INFO = {
  show: false,
  idx: 0,
  name: "",
};
